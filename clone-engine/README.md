# 2urn · Clone Engine (v2.0)

Deterministic section cloning for Carrd. Replaces the old setInterval/polling approach.
Paste into Body End embed block.

## Usage
Call `CarrdClone.clone(sourceId, targetId)` to clone a section.
